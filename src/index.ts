import { VMRemoteControlProvider } from './VMRemoteControlProvider.js';

// Export the provider class for OpenClaw's plugin loader
export default VMRemoteControlProvider;

export * from './types.js';
export * from './VMRemoteControlProvider.js';
